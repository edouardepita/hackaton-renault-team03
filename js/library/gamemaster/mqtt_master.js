//var client = mqtt.connect(broker, options);

function master_help(){
    console.log('Fonction disponible : \n' +
        'reset() => réinitialise la map \n' +
        'teleport() => téléport l\'agent \n' +
        '')
}

